{
  "name": "provider",
  "displayName": "Provider",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "provider.manage",
        "title": "Manage Provider API Key"
      }
    ],
    "languageModelChatProviders": [
      {
        "vendor": "provider",
        "displayName": "Provider",
        "managementCommand": "provider.manage"
      }
    ],
    "chatParticipants": [
      {
        "id": "provider.google",
        "name": "google",
        "fullName": "Google Search",
        "description": "What can I help you with?",
        "isSticky": true
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.3",
    "@types/vscode": "^1.107.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "@modelcontextprotocol/sdk": "^1.25.1",
    "@vscode/chat-extension-utils": "^0.0.0-alpha.5"
  }
}
